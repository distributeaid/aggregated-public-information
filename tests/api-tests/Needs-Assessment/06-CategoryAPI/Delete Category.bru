meta {
  name: Delete Category
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/categories/{{category_document_id}}
  body: none
  auth: inherit
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.be.oneOf([200, 204]);
  });
  
}
