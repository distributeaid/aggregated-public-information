meta {
  name: Delete Region Details
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/api/regions/{{region_document_id}}
  body: json
  auth: none
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.be.oneOf([200, 204]);
  });
  
  
}
