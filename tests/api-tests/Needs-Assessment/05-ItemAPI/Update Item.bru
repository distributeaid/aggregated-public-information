meta {
  name: Update Item
  type: http
  seq: 3
}

put {
  url: {{base_url}}/api/items/{{item_document_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "data": {
      "age_gender": "New Dummy age_gender",
      "size_style": "New Dummy size_style"
    }
  }
}

tests {
  test("Status code is 200 or 201", function () {
    expect(res.getStatus()).to.be.oneOf([200, 201]);
  });
  
  test("Correct information saved", function () {
    var jsonData = res.getBody();
    expect(jsonData.data.name).to.eql('DummyItem');
    expect(jsonData.data.age_gender).to.eql('New Dummy age_gender');
    expect(jsonData.data.size_style).to.eql('New Dummy size_style');
  });
  
}
